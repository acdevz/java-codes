package math;

public class Modulo {
    /* Properties of modulo...
    * (a + b) % m  =  ((a % m) + (b % m)) % m
    * (a - b) % m  =  ((a % m) - (b % m) + m) % m
    * (a * b) % m  =  ((a % m) * (b % m)) % m
    * (a / b) % m  =  ((a % m) * ((1 / b) % m)) % m
    */
}
